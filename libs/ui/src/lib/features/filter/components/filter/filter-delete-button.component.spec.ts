import { FilterDeleteButtonComponent } from './filter-delete-button.component';

describe('FilterDateComponent', () => {
  let component: FilterDeleteButtonComponent;

  beforeEach(async () => {
    component = new FilterDeleteButtonComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
